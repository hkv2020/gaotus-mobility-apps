diff -ruN passenger/lib/config/app_config.dart passenger/lib/config/app_config.dart
--- passenger/lib/config/app_config.dart	2026-09-23 10:49:09.000000000 +0000
+++ passenger/lib/config/app_config.dart	2026-09-23 11:27:49.729791034 +0000
@@ -1,6 +1,6 @@
 final class AppConfig {
   static const appName = 'Gaotus Mobility';
-  static const version = '0.4.2';
+  static const version = '0.4.3';
   static const defaultServer = 'https://beaphoenix.eu';
   static const namespace = '/wp-json/gaotus-mobility/v1';
 
diff -ruN passenger/lib/screens/home_screen.dart passenger/lib/screens/home_screen.dart
--- passenger/lib/screens/home_screen.dart	2026-09-23 10:49:32.000000000 +0000
+++ passenger/lib/screens/home_screen.dart	2026-09-23 11:27:49.729927710 +0000
@@ -140,7 +140,7 @@
             ListTile(leading: const Icon(Icons.refresh_rounded), title: const Text('Refresh rides'), onTap: () { Navigator.pop(sheetContext); s.refreshBookings(); }),
             ListTile(leading: const Icon(Icons.help_outline_rounded), title: const Text('Help & safety'), subtitle: const Text('Trip support and safety tools'), onTap: () => Navigator.pop(sheetContext)),
             const Divider(),
-            const ListTile(leading: Icon(Icons.info_outline_rounded), title: Text('Passenger v0.4.2'), subtitle: Text('Places suggestions + map fit fix')),
+            const ListTile(leading: Icon(Icons.info_outline_rounded), title: Text('Passenger v0.4.3'), subtitle: Text('Pickup pin + Places suggestions + ride map markers')),
             ListTile(leading: const Icon(Icons.logout_rounded), title: const Text('Sign out'), onTap: () { Navigator.pop(sheetContext); s.logout(); }),
           ]),
         ),
@@ -219,18 +219,44 @@
                     decoration: BoxDecoration(color: const Color(0xFFF3F3F1), borderRadius: BorderRadius.circular(22)),
                     padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 4),
                     child: Column(children: <Widget>[
-                      TextField(
-                        controller: pickup,
+                      InkWell(
+                        borderRadius: BorderRadius.circular(18),
                         onTap: choosePickup,
-                        readOnly: true,
-                        decoration: const InputDecoration(fillColor: Colors.transparent, prefixIcon: Icon(Icons.radio_button_checked, size: 18, color: Colors.blue), hintText: 'Pick-up location', border: InputBorder.none, enabledBorder: InputBorder.none, focusedBorder: InputBorder.none),
+                        child: Padding(
+                          padding: const EdgeInsets.symmetric(vertical: 14),
+                          child: Row(children: <Widget>[
+                            const SizedBox(width: 12),
+                            const Icon(Icons.radio_button_checked, size: 18, color: Colors.blue),
+                            const SizedBox(width: 13),
+                            Expanded(child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: <Widget>[
+                              const Text('Pick-up', style: TextStyle(fontSize: 11, color: AppTheme.softInk, fontWeight: FontWeight.w700)),
+                              const SizedBox(height: 2),
+                              Text(pickup.text.isEmpty ? 'Choose exact pick-up' : pickup.text, maxLines: 1, overflow: TextOverflow.ellipsis, style: const TextStyle(fontSize: 16, fontWeight: FontWeight.w800)),
+                            ])),
+                            const Icon(Icons.edit_location_alt_outlined, size: 21),
+                            const SizedBox(width: 12),
+                          ]),
+                        ),
                       ),
-                      const Divider(),
-                      TextField(
-                        controller: dropoff,
-                        readOnly: true,
+                      const Divider(height: 1),
+                      InkWell(
+                        borderRadius: BorderRadius.circular(18),
                         onTap: chooseDestination,
-                        decoration: const InputDecoration(fillColor: Colors.transparent, prefixIcon: Icon(Icons.stop_rounded, size: 19), hintText: 'Where are you going?', suffixIcon: Icon(Icons.search_rounded), border: InputBorder.none, enabledBorder: InputBorder.none, focusedBorder: InputBorder.none),
+                        child: Padding(
+                          padding: const EdgeInsets.symmetric(vertical: 14),
+                          child: Row(children: <Widget>[
+                            const SizedBox(width: 12),
+                            const Icon(Icons.stop_rounded, size: 19),
+                            const SizedBox(width: 13),
+                            Expanded(child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: <Widget>[
+                              const Text('Destination', style: TextStyle(fontSize: 11, color: AppTheme.softInk, fontWeight: FontWeight.w700)),
+                              const SizedBox(height: 2),
+                              Text(dropoff.text.isEmpty ? 'Search address, postcode or airport' : dropoff.text, maxLines: 1, overflow: TextOverflow.ellipsis, style: TextStyle(fontSize: 16, fontWeight: FontWeight.w800, color: dropoff.text.isEmpty ? AppTheme.softInk : AppTheme.ink)),
+                            ])),
+                            const Icon(Icons.search_rounded, size: 22),
+                            const SizedBox(width: 12),
+                          ]),
+                        ),
                       ),
                     ]),
                   ),
diff -ruN passenger/lib/screens/place_search_screen.dart passenger/lib/screens/place_search_screen.dart
--- passenger/lib/screens/place_search_screen.dart	2026-09-23 10:49:32.000000000 +0000
+++ passenger/lib/screens/place_search_screen.dart	2026-09-23 11:27:49.729996714 +0000
@@ -66,6 +66,7 @@
         decoration:InputDecoration(prefixIcon:const Icon(Icons.search_rounded),hintText:'Search address, airport or place',suffixIcon:controller.text.isEmpty?null:IconButton(onPressed:(){controller.clear();changed('');setState((){});},icon:const Icon(Icons.close_rounded))),
       )),
       if(loading)const LinearProgressIndicator(minHeight:2),
+      if(controller.text.trim().length>=2&&!loading&&error==null) Padding(padding:const EdgeInsets.fromLTRB(20,8,20,4),child:Row(children:<Widget>[const Text('Suggestions',style:TextStyle(fontWeight:FontWeight.w800)),const Spacer(),Text('Google',style:Theme.of(context).textTheme.bodySmall?.copyWith(color:AppTheme.softInk,fontWeight:FontWeight.w700))])),
       if(error!=null)Padding(padding:const EdgeInsets.all(16),child:Text(error!,style:TextStyle(color:Theme.of(context).colorScheme.error))),
       Expanded(child:suggestions.isEmpty&&!loading&&error==null?Center(child:Padding(padding:const EdgeInsets.all(28),child:Text(controller.text.trim().length<2?'Start typing a street, postcode, airport or destination.':'No suggestions found. Try the full postcode, street name or airport.',textAlign:TextAlign.center,style:const TextStyle(color:AppTheme.softInk)))):ListView.separated(
         padding:const EdgeInsets.symmetric(vertical:6),itemCount:suggestions.length,separatorBuilder:(_,__)=>const Divider(height:1,indent:64),
diff -ruN passenger/lib/screens/trip_screen.dart passenger/lib/screens/trip_screen.dart
--- passenger/lib/screens/trip_screen.dart	2026-09-23 09:46:17.000000000 +0000
+++ passenger/lib/screens/trip_screen.dart	2026-09-23 11:27:49.730181082 +0000
@@ -131,8 +131,8 @@
           final driverPoint = tracking?.lat != null && tracking?.lng != null ? LatLng(tracking!.lat!, tracking.lng!) : null;
           final center = driverPoint ?? pickup ?? const LatLng(51.5074, -0.1278);
           final markers = <Marker>[
-            if (pickup != null) Marker(point: pickup, width: 42, height: 42, child: const _TripPin(icon: Icons.circle, dark: true)),
-            if (dropoff != null) Marker(point: dropoff, width: 42, height: 42, child: const _TripPin(icon: Icons.stop_rounded, dark: false)),
+            if (pickup != null) Marker(point: pickup, width: 48, height: 56, child: const _TripPin(icon: Icons.location_on_rounded, dark: true)),
+            if (dropoff != null) Marker(point: dropoff, width: 44, height: 44, child: const _TripPin(icon: Icons.stop_rounded, dark: false)),
             if (driverPoint != null) Marker(point: driverPoint, width: 58, height: 58, child: _DriverMarker(live: tracking?.signal == 'live')),
           ];
 
@@ -141,7 +141,7 @@
               Positioned.fill(
                 child: FlutterMap(
                   mapController: map,
-                  options: MapOptions(initialCenter: center, initialZoom: 14.5),
+                  options: MapOptions(initialCenter: center, initialZoom: 14.5, initialCameraFit: driverPoint == null && pickup != null && dropoff != null ? CameraFit.bounds(bounds: LatLngBounds.fromPoints(<LatLng>[pickup, dropoff]), padding: const EdgeInsets.fromLTRB(50, 110, 50, 300), maxZoom: 15.5) : null),
                   children: <Widget>[
                     TileLayer(urlTemplate: 'https://tile.openstreetmap.org/{z}/{x}/{y}.png', userAgentPackageName: 'com.gaotus.mobility.passenger'),
                     MarkerLayer(markers: markers),
@@ -175,7 +175,7 @@
                         const SizedBox(height: 16),
                         const LinearProgressIndicator(minHeight: 5, borderRadius: BorderRadius.all(Radius.circular(99))),
                       ],
-                      if (tracking != null && !const <String>{'completed','cancelled','canceled'}.contains(booking.status.toLowerCase())) ...<Widget>[const SizedBox(height: 14), _TrackingBanner(tracking: tracking)],
+                      if (driver != null && tracking != null && !const <String>{'completed','cancelled','canceled'}.contains(ride.status.toLowerCase())) ...<Widget>[const SizedBox(height: 14), _TrackingBanner(tracking: tracking)],
                       if (driver != null) ...<Widget>[
                         const SizedBox(height: 18),
                         _DriverCard(
@@ -323,10 +323,14 @@
   const _DriverMarker({required this.live});
   final bool live;
   @override
-  Widget build(BuildContext context) => Container(
-        decoration: BoxDecoration(color: Colors.white, shape: BoxShape.circle, border: Border.all(color: live ? AppTheme.success : AppTheme.softInk, width: 2.5), boxShadow: const <BoxShadow>[BoxShadow(color: Color(0x33000000), blurRadius: 10)]),
-        child: const Icon(Icons.local_taxi_rounded, color: AppTheme.ink, size: 31),
-      );
+  Widget build(BuildContext context) => Stack(clipBehavior: Clip.none, alignment: Alignment.center, children: <Widget>[
+        Container(
+          width: 54, height: 42,
+          decoration: BoxDecoration(color: Colors.white, borderRadius: BorderRadius.circular(14), border: Border.all(color: AppTheme.ink, width: 2), boxShadow: const <BoxShadow>[BoxShadow(color: Color(0x33000000), blurRadius: 10)]),
+          child: const Icon(Icons.local_taxi_rounded, color: AppTheme.ink, size: 30),
+        ),
+        Positioned(right: -1, top: -3, child: Container(width: 13, height: 13, decoration: BoxDecoration(color: live ? AppTheme.success : AppTheme.softInk, shape: BoxShape.circle, border: Border.all(color: Colors.white, width: 2)))),
+      ]);
 }
 
 class _TrackingBanner extends StatelessWidget {
diff -ruN passenger/pubspec.yaml passenger/pubspec.yaml
--- passenger/pubspec.yaml	2026-09-23 10:49:09.000000000 +0000
+++ passenger/pubspec.yaml	2026-09-23 11:27:49.729642440 +0000
@@ -1,7 +1,7 @@
 name: gaotus_mobility_passenger
 description: Passenger app for Gaotus Mobility Platform.
 publish_to: none
-version: 0.4.2+6
+version: 0.4.3+7
 environment:
   sdk: '>=3.4.0 <4.0.0'
 dependencies:
